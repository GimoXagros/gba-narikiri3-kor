# 아이템 설명 대조와 보완

2026-09-09, v1.1a-dev28. 일본판과 v1.1의 아이템 123개 설명을 같은 ID별로 대조해 35개 오역·표기 차이를 보완했다. 아이템 이름 자체의 123개 번역과는 별도 필드다. 개별 채택 근거는 `translations/item_descriptions.json`에 기록한다.

ID 52 스타 메이스의 v1.1 설명 `1074C0`에는 엘븐 보우(ID 51)의 활 설명·남성 표시·체스터 이름이 복제돼 있었다. 일본판 같은 위치는 치유의 힘이 있는 법술사 지팡이·여성 전용 민트 루트다. 두 항목은 원래 서로 다른 포인터이며 설명 내용을 잘못 복제한 문제다. ID 57 렌즈의 혜성도 v1.1에서는 행성 수성으로 번역돼 있었다. 현상을 사상으로 옮긴 사전, 암반 굴착을 절벽용으로 옮긴 드릴과 인물·코스튬 표기도 수정했다. 회복량 30%/60% 등 원래 수치는 유지한다.

## 소비자와 보존 범위

`105758`의 24바이트 레코드 123개는 `65BC`에서 조회한다. `65D0`은 개수 123, `65EC`은 이름, `6600`은 +4 설명 포인터를 반환한다. 다섯 실제 설명 경로는 `CABB4..CABCE`, `CB18E..CB1A8`, `CB21E..CB22E`, `D1468..D149C`, `D9328..D934C`다. 목록 ID 조회, 기존 표시 지우기, `%0t%l%s` 포매터 또는 직접 출력, 글자 대기 설정 복원 등 각 원래 동작을 유지한다.

35개의 +4 포인터만 새 문자열 `1120000..112066D`로 연결한다. 원래 설명 원본, 나머지 88개의 설명 포인터, 아이템 효과·분류·가격·제작 관련 +8..+17 바이트와 호출 코드를 보존한다. 모든 새 문장은 원래 18칸×2줄에 들어가며 포매터 명령을 추가하지 않는다.

## 검사와 직접 관찰

정확한 ROM SHA-256은 `b21df6f6b0308c5d3b0a5d9bbc8081b5f6abea9e76f0755ef68a55d0f17be64a`다. 일본판+제공 IPS의 누적 빌드와 단독 수정 시제품이 바이트 동일하고 누적 분리 CPU 검사를 통과했다.

전체 123개 설명의 실제 조회 246회, 다섯 경로×두 모드 출력 1230회, 글자 위치 26350개를 검사했다. 전체 3840바이트 출력 공간은 원본 12×16 글자 비트로 독립 계산한 값과 일치한다. BIOS 고정 글꼴 공간 채우기만 모형화하며 목록 선택과 글자 대기 0은 분리 CPU 시험 조건이다. 35개 실제 CPU 출력을 다섯 장으로 그려 여성 표시/민트, 혜성, 코스튬 이름과 두 줄 배치를 직접 확인했다. 정상 플레이로 전부 획득한 아이템 화면과는 구별한다.

첫 그림에서는 검사기의 `03001464` 보호용 A5 값 때문에 글자가 덩어리로 보였다. 이 주소는 실제 색 변환표다. 게임의 `00 0F F0 FF`와 다른 모드의 `BB BF FB FF`로 초기화하고 모든 픽셀을 비교하도록 검사기를 강화했다. ROM 변경은 없었다. 같은 이유로 기술 이름/설명·도감 소개·음악 제목 검사도 글자 위치 확인에 독립 픽셀 비교를 추가했고 dev28에서 각각 통과했다.

강화된 아이템 보고서는 `output/v1.1a-dev28-verified/item-description-pixel-verification.json`, 직접 관찰은 `post-build-item-description-evidence.json`에 있다. 최초 `reproduction.json`을 수정하지 않고 이후 검사 도구와 결과 해시는 보완 증거에 따로 기록한다. 정상 획득·제작·다른 메뉴 상태와 PC 전편 검토는 남아 있으며 전체 번역 완료 선언이 아니다.
