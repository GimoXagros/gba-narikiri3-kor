# 기술 이름·설명 보완

2026-09-09, dev22. 일본판의 741DDC, 20 bytes × 390개 기술 레코드를 기존 v1.1과 대조했다. +0 소형 이름, +4 대형 이름, +8 설명은 텍스트 포인터이며 +C..13의 효과·비용 속성은 수정하지 않는다. 원래 6E3C 이름 조회와 6E68 설명 조회, CA572..CA590 설명/줄바꿈 출력 경로를 고정 바이트로 검사한다.

기존의 명확한 오역·누락 기술명 80개를 추가 수정했다. 예: 색인화→인디그네이션, 창문→윈드 애로, 간호사→너스, 미친 커밋→크레이지 코멧, 열렬참→열공참, 천탄→천상탄. 전작 v0.9b의 `banked_arte_names.tsv`를 용어 참고로 사용하며 두 게임의 ID를 그대로 대응시키지 않는다. 사용자 자료의 이터널 스로우/지라이야도 반영한다. 작은 이름과 큰 이름을 함께 수정해 메뉴 간 차이를 줄였다. 기존 18개 수리를 합하면 대형 이름 선택은 98개다.

설명 50개를 원문의 대상·수량·효과에 맞춰 보완했다. 잘린 ‘관통하는 에너지탄을 발’, ‘잠시 인챈율을 낮’, 운반으로 오역된 주사위 효과 등을 고쳤다. 피해 주사는 **현재 HP의 10%**라는 기준을 명시했다. 숫자·대상·상태이상과 원래 기술 데이터는 유지한다. 최대 18개 표시 칸을 넘기지 않아 다음 TP/속성 줄을 침범하지 않도록 했다.

`source/skill_text_profile.json`, `translations/skills.json`에 실제 표·호출·원본 문자열 해시와 수정 근거가 있다. 검색 결과에서 새 항목을 자동 채택하지 않는다. 설명이 ‘미사용’인 원작 슬롯은 도달 검증 없이 일반 플레이의 미번역으로 세지 않는다. 390개 표를 읽은 것과 모든 기술의 습득·사용·화면 검증은 다르다.

검사: dev22, SHA-256 42921a847856f78eac2ed2f815bd045a5b904d32dcaeb1199917baa6d9d89030. 390개 레코드의 비텍스트 속성과 미선택 포인터 보존, 이름 조회 1560회, 대형 이름 780회, 실제 설명/문자열 길이/조건부 줄바꿈 780회, 글자 위치 13644개를 통과했다. 검사에서는 실제 설명 출력 후 원래 TP 계산 직전에 멈춰 후속 표시 위치를 확인했다. TP 수치 계산을 대체 구현하거나 변경하지 않았다. 큰 글꼴 버퍼 경계도 유지됐다. 정상 게임의 추가 기술 화면 관찰은 진행 중이다.
