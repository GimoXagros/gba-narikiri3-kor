# 리조또 설명 한 줄 넘침 수정

2026-09-13, v1.1a dev36.

기존 보완문 ‘쌀을 국물에 넣어 죽처럼 끓인 요리’는 전각 공백 포함 19칸이었다. 실제 두 줄 창은 첫 줄 18칸을 설명, 다음 줄을 효과에 사용한다. 설명이 자동 줄바꿈된 뒤 효과가 출력되어 앞부분이 밀리고 마지막 ‘리’만 남는 현상을 dev35에서 재현했다.

사용자 지정 문장 **국물에 쌀을 넣어 끓인 요리**로 변경했다. 전각 공백 포함 15칸·180px·종결 00 포함 31바이트로 한 줄에 들어간다. 일본판 원문은 ‘ライスをスープに入れた雑炊風の料理’다. 원래 자료와 의미를 유지하며 요청 문장을 채택했다.

`recipe_text.py`의 설명 제한을 28칸에서 18칸으로 바로잡았다. `verify_recipes.py`는 기존 22개 레코드의 소비자 검사와 함께 전체 설명의 한 줄 길이를 검사한다. 기존 인코딩에서 만든 명시적 표로 별도 길이 검사도 통과했다.

mGBA와 VBA Next에서 해금 검토 저장을 읽고 일반 버튼으로 22개 요리 설명을 모두 표시했다. 두 코어에서 모든 설명·효과 글자의 위치가 같고 넘침이 없다. 같은 mGBA의 dev35와 22개 화면을 비교하면 리조또 첫 줄의 455픽셀만 달라지고 효과 ‘HP60%회복’, 재료·숫자·나머지 21개 설명은 같다. 코어 사이의 배경 RGB 변환 차이는 글자의 검은 픽셀 마스크 비교와 구분했다. 정상 획득이나 요리 성공 결과 검증을 뜻하지 않는다.

최종 ROM은 `output/v1.1a-dev36-verified-01/ND3_B3TJ_K_v1.1a.gba`, SHA-256 `c45a9b0e9c974da8f63f707a5a9a983ddacff0f7d5079505cd9ddeddcf3f4d11`. 원본과 기존 수정판은 보존했다. ROM 변경은 요리 문자열 저장 영역과 그 포인터에 한정되며 효과/재료 바이트를 보존한다. 누적 검사 29개와 두 코어의 첫 저장·별도 프로세스 재로드를 확인했다.

`logo.png`는 사용자가 이번에 제공한 Desktop 파일로 그대로 교체했다. SHA-256 `925a32f439347150a5f1633849ee7daeb44cbc5e3abcbb61a2fd9dbe6aeee1ae`. GitHub 소개용 자산이며 ROM 내부 로고 변경이 아니다.

정확한 이미지·ROM·코어·길이 검사와 대사 언어 검색 근거는 `qa/results/recipe-layout-dev36/`에 있다. 전체 미번역 여부의 한계는 `TEXT_POPULATION.md`의 dev36 항목을 참조한다.
